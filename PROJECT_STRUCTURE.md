# 📁 项目结构

```
SEC-EDGAR-ScriptGen/
│
├── 📂 app/                          # Next.js App Router 应用目录
│   ├── 📂 api/                      # API 路由
│   │   └── 📂 generate/
│   │       └── route.ts             # POST /api/generate - Gemini AI 脚本生成端点
│   │
│   ├── 📂 components/               # React 组件
│   │   ├── CodeViewer.tsx           # 代码显示和复制组件
│   │   └── ConfigForm.tsx           # 用户输入表单组件
│   │
│   ├── globals.css                  # 全局样式（Tailwind + 自定义）
│   ├── layout.tsx                   # 根布局（HTML 结构 + 字体）
│   ├── page.tsx                     # 主页（应用入口）
│   └── types.ts                     # TypeScript 类型定义
│
├── 📂 public/                       # 静态资源目录（图片、字体等）
│
├── 📄 .env.example                  # 环境变量示例文件
├── 📄 .eslintrc.json                # ESLint 配置
├── 📄 .gitignore                    # Git 忽略文件列表
│
├── 📄 next.config.js                # Next.js 框架配置
├── 📄 next-env.d.ts                 # Next.js TypeScript 类型声明
├── 📄 package.json                  # NPM 依赖和脚本
├── 📄 postcss.config.js             # PostCSS 配置（用于 Tailwind）
├── 📄 tailwind.config.js            # Tailwind CSS 配置
├── 📄 tsconfig.json                 # TypeScript 配置
│
├── 📄 README.md                     # 项目说明文档
├── 📄 QUICKSTART.md                 # 快速开始指南
├── 📄 INSTALLATION.md               # 详细安装指南
├── 📄 DEPLOYMENT.md                 # 部署指南
├── 📄 PROJECT_STRUCTURE.md          # 本文件 - 项目结构说明
│
├── 📄 start.sh                      # Linux/macOS 快速启动脚本
└── 📄 start.bat                     # Windows 快速启动脚本
```

---

## 📋 核心文件说明

### 🔧 配置文件

#### `next.config.js`
Next.js 主配置文件，控制框架行为。
```javascript
module.exports = {
  reactStrictMode: true,  // 启用 React 严格模式
}
```

#### `tsconfig.json`
TypeScript 编译配置，定义类型检查规则和路径别名。
```json
{
  "paths": {
    "@/*": ["./*"]  // 允许使用 @/ 作为根目录别名
  }
}
```

#### `tailwind.config.js`
Tailwind CSS 配置，定义扫描路径和主题扩展。

#### `package.json`
项目依赖和脚本命令：
- `npm run dev` - 开发服务器
- `npm run build` - 生产构建
- `npm start` - 启动生产服务器
- `npm run lint` - 代码检查

---

### 🎨 应用文件

#### `app/layout.tsx`
根布局组件，包含：
- HTML 文档结构
- 元数据配置（标题、描述）
- 全局字体加载
- 全局样式引入

#### `app/page.tsx`
主页面组件（首页），包含：
- 应用主逻辑
- 状态管理（加载状态、错误处理）
- API 调用逻辑
- 布局结构（导航栏、表单、代码显示区域）

#### `app/globals.css`
全局样式，包括：
- Tailwind CSS 导入
- 自定义滚动条样式
- 动画定义（fadeIn）
- 基础样式重置

#### `app/types.ts`
TypeScript 类型定义：
```typescript
interface ScriptConfig {
  developerName: string
  developerEmail: string
  targetData: string
}

interface GeneratedScript {
  code: string
  explanation: string
}

enum LoadingState {
  IDLE, LOADING, SUCCESS, ERROR
}
```

---

### 🧩 组件文件

#### `app/components/ConfigForm.tsx`
用户输入表单组件，功能包括：
- 开发者信息输入（姓名、邮箱）
- 数据需求描述输入
- 表单验证
- 提交按钮（带加载状态）
- UI 反馈（禁用状态、加载动画）

**Props:**
- `onGenerate: (config: ScriptConfig) => void` - 提交回调
- `loadingState: LoadingState` - 加载状态

#### `app/components/CodeViewer.tsx`
代码查看器组件，功能包括：
- 代码语法高亮显示
- 复制到剪贴板功能
- 策略说明展示
- 空状态提示
- 动画过渡效果

**Props:**
- `data: GeneratedScript | null` - 生成的脚本数据

---

### 🔌 API 路由

#### `app/api/generate/route.ts`
后端 API 端点，处理 AI 脚本生成。

**功能：**
- 接收前端 POST 请求
- 验证请求数据
- 调用 Gemini API（密钥安全存储在后端）
- 构造提示词
- 解析 AI 响应
- 返回生成的 Python 代码和说明

**请求格式：**
```typescript
POST /api/generate
Content-Type: application/json

{
  "developerName": "张三",
  "developerEmail": "zhangsan@example.com",
  "targetData": "获取苹果公司的最新 10-K 报告"
}
```

**响应格式：**
```typescript
{
  "code": "import requests\n...",
  "explanation": "该脚本使用..."
}
```

**错误处理：**
- 400: 缺少必需字段
- 500: API 密钥未配置或 Gemini 调用失败

---

## 🔐 安全架构

```
┌─────────────────┐
│   浏览器 (前端)  │
│                 │
│  - ConfigForm   │
│  - CodeViewer   │
│                 │
└────────┬────────┘
         │ HTTPS
         │ POST /api/generate
         │ { config }
         ▼
┌─────────────────┐
│ Next.js 服务器   │
│                 │
│  - API Route    │───┐
│  - 验证请求      │   │ 读取环境变量
│  - 调用 Gemini  │◄──┤ GEMINI_API_KEY
│  - 返回结果     │   │ (.env.local)
│                 │   │
└────────┬────────┘   │
         │            │
         │ HTTPS      │
         ▼            │
┌─────────────────┐   │
│  Gemini API     │   │
│                 │   │
│  - 生成代码     │   │
│  - 返回 JSON    │   │
│                 │   │
└─────────────────┘   │
                      │
                      ▼
              🔒 API 密钥永远不暴露给前端
```

**关键点：**
1. ✅ API 密钥仅在服务器端使用
2. ✅ 前端无法访问 `process.env.GEMINI_API_KEY`
3. ✅ 所有 AI 调用通过后端代理
4. ✅ 环境变量文件 (`.env.local`) 不会被 Git 跟踪

---

## 🎯 数据流

```
用户输入
   ↓
ConfigForm 组件
   ↓
app/page.tsx (handleGenerate)
   ↓
fetch('/api/generate', { POST, config })
   ↓
app/api/generate/route.ts
   ↓
验证数据
   ↓
构造 Gemini 提示词
   ↓
调用 Gemini API
   ↓
解析响应 JSON
   ↓
返回 { code, explanation }
   ↓
app/page.tsx (setScriptData)
   ↓
CodeViewer 组件
   ↓
显示生成的代码
```

---

## 📦 依赖关系

### 生产依赖
```json
{
  "react": "^18.3.1",                    // UI 框架
  "react-dom": "^18.3.1",                // React DOM 渲染
  "next": "^15.0.3",                     // Next.js 框架
  "@google/generative-ai": "^0.21.0",   // Gemini AI SDK
  "lucide-react": "^0.555.0"            // 图标库
}
```

### 开发依赖
```json
{
  "@types/node": "^22.14.0",
  "@types/react": "^18.3.12",
  "@types/react-dom": "^18.3.1",
  "typescript": "~5.8.2",
  "tailwindcss": "^3.4.15",
  "postcss": "^8.4.49",
  "autoprefixer": "^10.4.20",
  "eslint": "^8.57.0",
  "eslint-config-next": "^15.0.3"
}
```

---

## 🔄 开发工作流

1. **本地开发**
   ```bash
   npm run dev              # 启动开发服务器
   # 编辑代码，浏览器自动刷新
   ```

2. **构建测试**
   ```bash
   npm run build            # 构建生产版本
   npm start                # 测试生产版本
   ```

3. **代码检查**
   ```bash
   npm run lint             # ESLint 检查
   ```

4. **部署**
   ```bash
   git push                 # 推送到 GitHub
   # Vercel 自动部署
   ```

---

## 🛠️ 扩展建议

### 添加新功能

1. **添加新的 API 端点**
   - 在 `app/api/` 下创建新文件夹
   - 例如：`app/api/validate/route.ts`

2. **添加新页面**
   - 在 `app/` 下创建新文件夹
   - 例如：`app/about/page.tsx` → `/about` 路由

3. **添加新组件**
   - 在 `app/components/` 中创建新文件
   - 例如：`app/components/Header.tsx`

4. **添加新的类型**
   - 在 `app/types.ts` 中定义
   - 或创建新的类型文件：`app/types/api.ts`

### 性能优化

1. **使用 React Server Components**
   - 默认所有组件都是服务器组件
   - 添加 `'use client'` 指令来创建客户端组件

2. **代码分割**
   - Next.js 自动进行路由级代码分割
   - 使用 `dynamic` 导入大型组件

3. **图片优化**
   - 使用 Next.js `<Image>` 组件
   - 自动优化和懒加载

---

## 📚 相关文档

- [README.md](./README.md) - 项目概览
- [QUICKSTART.md](./QUICKSTART.md) - 快速开始
- [INSTALLATION.md](./INSTALLATION.md) - 安装指南
- [DEPLOYMENT.md](./DEPLOYMENT.md) - 部署指南

---

**需要帮助？** 请查看文档或在 GitHub 上创建 Issue。

